## Plan

1. Edit `public/sitemap.xml`
   - Replace every occurrence of `https://peptide-mastery.lovable.app` with `https://peptide-south-africa.co.za`
   - Add `<lastmod>2026-06-20</lastmod>` inside every `<url>` block
2. Publish the project via `preview_ui--publish`