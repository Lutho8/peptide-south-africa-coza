// Paywall removed — bloodwork is free for all signed-in users. Stub returns nothing
// so callers' conditional rendering keeps working but never shows a gate.
export function PremiumGate(_props: { onSignIn: () => void }) {
  return null;
}
